<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.voter">

    <uses-permission android:name="android.permission.SEND_SMS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/revv"
        android:label="@string/app_name"
        android:roundIcon="@drawable/revv"
        android:supportsRtl="true"
        android:theme="@style/Theme.Voter">
        <activity android:name=".splashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".login">

        </activity>
        <activity android:name=".signup" />
        <activity android:name=".send_otp" />
        <activity android:name=".otp_verification" />
        <activity android:name=".face_detection"></activity>
        <activity android:name=".Voting_Activity"></activity>
        <activity android:name=".end_Activity" />
    </application>

</manifest>